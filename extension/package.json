{
  "name": "@design-easily/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "build:prod": "NODE_ENV=production vite build",
    "zip": "npm run build:prod && cd dist && zip -r ../design-easily.zip . && echo 'Created design-easily.zip'",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.22.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@types/chrome": "^0.0.268",
    "vite": "^8.0.9"
  }
}
